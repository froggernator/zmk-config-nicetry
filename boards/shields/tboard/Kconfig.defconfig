if SHIELD_TBOARD_LEFT
  
config ZMK_KEYBOARD_NAME
    default "tBoard"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_TBOARD_LEFT || SHIELD_TBOARD_RIGHT

config ZMK_SPLIT
    default y

endif
